Data component revamp:

  - Make the DataCatcher work with second layer, and TwoLayer models
  - Revamp the DataMaker
  - Delete old DataClasses (PreProcessor, Processor, Old Data Reader, DataCache)
  - Change DataOutputWriter (either inherit from Writer, but anyways change how it's handle in Tools and models)
  - DataCatcher with CSV
  - DataMaker with test


Final Submission:
  - Workflow if want to add more data (Config + Catcher)
  - Diagram for classes relationship (Blue: class_name, Black:What the class do, Arrow:relationship (inheritance or use))
  - Document future enhancement (New Model, Pipelin, MultiLayer, New data)
  -
